<?xml version="1.0" encoding="utf-8"?>
<layout>
  <data>
    <import type="com.company.takitate.viewmodel.RecruitAPIResponseShopViewModel"/>
    <variable name="viewModel" type="RecruitAPIResponseShopViewModel" />
  </data>
  <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:id="@+id/contentBox"
    android:background="@drawable/android_gradient_list"
    android:layout_height="wrap_content">

    <TextView
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="@{viewModel.focusedShop.id}" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
      android:id="@+id/fab"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="end|bottom"
      android:src="@drawable/ic_baseline_add_24"
      android:layout_margin="16dp" />

  </LinearLayout>
</layout>

